__pycache__/
*.pyc
venv/
.env
.env.local
*.log
logs/
uploads/*
!uploads/.gitkeep
ssl/*.pem
ssl/*.key
instance/
flask_session/
# /models/ at project root was used for ad-hoc ML artifacts. The bare
# `models/` pattern was global and silently swallowed app/core/models/ —
# constrain it to the repo root.
/models/

# IDE / tooling
.claude/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Babel build artifact (regenerated from source by pybabel extract)
translations/messages.pot
